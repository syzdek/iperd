label memtest86_label
    MENU LABEL Memtest86+
    MENU DISABLE

label memtest
    MENU LABEL Memtest86+ @VERSION@
    TEXT HELP
    Perform memory test on system.

    Press [TAB] to append required boot options (i.e. console=ttyS1,115200).
    ENDTEXT
    linux /boot/memtest/memtest.bin

