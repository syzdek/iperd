label ubnt@VERSION@
    MENU LABEL Ubuntu Linux @VERSION@ "@CODENAME@" Install (64 bit)
    TEXT HELP
    Boot 64 bit Ubuntu installer for system builds or troubleshooting.

    Press [TAB] to append required boot options (i.e. console=ttyS1,115200).
    ENDTEXT
    kernel /boot/ubuntu/@VERSION@/@ARCH@/vmlinuz
    initrd /boot/ubuntu/@VERSION@/@ARCH@/initrd
    append -- quiet

